Visual Studio Code
git scm

github.com/Area51TrainingCenter/FullStackDeveloper_Group02

sergiohidalgocaceres@gmail.com

no es necesario utilizar el punto y coma

Consideraciones para subir a GitHub:
Crear un repositorio en Github
Copiar el link del repositorio creado
Clonarlo en VS Code

ubicarnos en la carpeta cd "Clase 01" y ejecutar node nombrearchivo.js
para ejecutar el archivo de JS debe estar instalado el node

View - Toogle Panel para ejecutar el JS

usar camelCase

escribir en code: html:5  y automaricamente escribe la estructura html

if ( typeof(valor) === "string" )
correo usando expresiones regular: var correoValido = correo.match()
if !correo new throw Error('correo no valido')
